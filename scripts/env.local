# Auto-created for local runs; not committed (see .gitignore).
# Password must stay quoted: # starts a comment in shell.

E2E_EMAIL=shahzaib@ualberta.ca
E2E_PASSWORD='#TestPassword'

# So a fresh E2E SQLite DB can register/login on first API boot
AUTH_SIGNUP_ALLOWLIST='shahzaib@ualberta.ca:CityPlanner|OperationalManager'
