# Local-only env for Codex / UI e2e helpers. Not committed.
# First time: ./scripts/init-env.sh
# Then edit scripts/env.local and set E2E_PASSWORD.

E2E_EMAIL=
E2E_PASSWORD=

# Optional: bootstrap this user when the E2E API starts with an empty SQLite file
# (quote the value — | is a shell pipe when this file is sourced)
# AUTH_SIGNUP_ALLOWLIST='you@example.com:CityPlanner|OperationalManager'
