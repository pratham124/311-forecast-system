export function UserGuideLoadingState() {
  return (
    <section className="mt-6 rounded-[24px] border border-slate-200 bg-slate-50 px-5 py-6" aria-live="polite">
      <p className="text-sm font-semibold text-ink">Loading the user guide...</p>
      <p className="mt-2 text-sm leading-6 text-muted">Preparing the latest instructions and section navigation.</p>
    </section>
  );
}
